{
  "name": "tntjs",
  "version": "0.1.0",
  "description": "TNT.js is a Javascript-based programming language. It's our goal to make it as simple as Python, and at the same time make it looks like a front-end programming language. The language avoid the cons of TypeScript that can't run directly on the browser.",
  "scripts": {
    "build": "tsc  && javascript-obfuscator dist/tnt.js --output dist/tnt.min.js --string-array-encoding 'rc4' && rm dist/tnt.js",
    "develop": "tsc --watch",
    "precommit": "rm dist/tnt.js && rm dist/tnt.d.ts && rm dist/tnt.js.map"
  },
  "repository": {
    "type": "git",
    "url": "https://github.com/Bug-Duck/tntjs.git"
  },
  "author": "BugDuck, 27Onion, Acbox",
  "license": "MIT",
  "dependencies": {
    "typescript": "^4.6.3"
  },
  "devDependencies": {
    "@typescript-eslint/eslint-plugin": "^5.26.0",
    "@typescript-eslint/parser": "^5.26.0",
    "eslint": "^8.16.0",
    "javascript-obfuscator": "^4.0.0"
  }
}
