namespace tntem {
    export function tpif(dom) {
        const HTMLCodes = dom.innerHTML;
        const be: Array<string> = [];
        switch (be[1]) {
            case 'equals':
                if (be[0] === be[1]) {

                };
                break;
            case 'unequls':
                if (be[0] !== be[2]) {

                };
                break;
        }
    }
}